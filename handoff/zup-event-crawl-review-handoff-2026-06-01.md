# Zup 活动抓取审核台交接

更新时间：2026-06-03 CST

## 交接目的

这个项目是在本地验证 Zup App 的城市活动冷启动流程：从第三方平台抓取城市活动，清洗成结构化数据，在本地审核台人工判断“通过 / 待定 / 拒绝”。当前只做本地验证，不对接线上发布接口。

当前用户特别重视两件事：

- 抓到的内容不要直接丢弃。即使规则判断不适合，也要保留在审核台里，默认拒绝即可，方便人工二次判断。
- `body` 活动简介只写活动本身，不要写审核判断，不要写“是否适合 Zup”“默认拒绝”“人工判断”等系统话术。

## 工作区与运行方式

项目目录：

`/Users/weiming/Documents/万盏街灯/开发物料/官网/BuzzInMap_website`

本地审核服务：

```bash
node scripts/review-server.js 8787
```

审核台地址：

`http://127.0.0.1:8787/events/crawl-review.html`

如果 8787 服务失效，重新运行上面的命令即可。

## 关键文件

- `events/crawl-review.html`
  - 本地人工审核页面。
  - 单列活动列表，每行一个活动。
  - 支持城市、状态、来源、类型、日期、排序、搜索筛选。

- `scripts/review-server.js`
  - 本地审核服务。
  - 负责静态页面、审核状态 API、图片代理 API。
  - 图片通过 `/api/image?src=...` 代理并缓存，避免第三方图床热链问题。

- `data/crawled-events.json`
  - 当前抓取活动数据，纯 JSON。

- `data/crawled-events.js`
  - 审核页面读取用的数据包装文件。

- `data/review-decisions.json`
  - 人工审核选择，持久化保存。
  - `approved / pending / rejected`。

- `data/approved-events.json`
  - 当前已通过活动列表，供后续发布接口使用。

- `data/image-cache/`
  - 本地图片代理缓存。

## 当前数据状态

当前审核台已切回多城市模式，`data/crawled-events.json` / `data/crawled-events.js` 里是合并数据：

- 北京：10 条
- 上海：30 条
- 当前总数：40 条

重要说明：

- 这 10 条北京数据是从本机临时快照里恢复回来的“已找回样本”，不是之前那份更完整的北京 20 条集。
- 上海 30 条是 2026-06-03 新抓的数据，按豆瓣分页原始顺序读取第 1-30 条，没有因为结束状态、低质判断或主观筛选补位。
- 当前页面已经支持城市筛选，默认可在“全部城市 / 北京 / 上海”之间切换。
- 豆瓣不是 Zup 活动库的唯一数据源，也不是长期唯一来源。它只是当前这轮本地验证流程里最先跑通的小样本来源，用来验证“抓取 -> 清洗 -> 摘要 -> 审核 -> 导出”的链路。审核台和数据结构已经按多来源、多城市预留，后续会继续接活动行、小红书、公众号、人工录入或其他授权/合作来源。

当前来源信息：

- 北京入口：`https://beijing.douban.com/events/week-all`
- 上海入口：`https://shanghai.douban.com/events/week-all`
- 默认展示顺序：各城市内部都按来源原始顺序
- 当前数据中低分内容仍保留，只是默认拒绝

数据字段核心约定：

- `id`：来源活动 ID。
- `source`：来源标识，例如 `douban`。
- `sourceName`：来源展示名，例如 `豆瓣同城`。
- `sourcePosition`：来源页原始顺序。
- `title`：活动标题。
- `startDate / endDate`：活动开始/结束时间。
- `eventDates`：活动覆盖的日期数组，用于按天筛选。跨多天活动要覆盖每一天。
- `location / latitude / longitude`：地点与经纬度。
- `image`：原始图片地址。
- `body`：面向用户的活动简介，不超过 400 字。
- `score`：规则分数。
- `suggested`：是否规则推荐。
- `reviewReason`：审核理由，可以包含“不适合 Zup/偏行业/票务”等判断。

当前多城市包装补充字段：

- `cities`：当前数据包里包含的城市列表，例如 `["北京", "上海"]`。
- `cityMeta`：每个城市自己的抓取时间、来源页和条数。
- `sourcePages`：按城市记录来源页 URL。

重要：`body` 和 `reviewReason` 要分开。活动简介里不能混入审核判断。

## 审核台行为

当前审核台规则：

- 已过期活动不显示。
  - 判断方式：`endDate` 早于当天则视为过期。
  - 过期活动不参与列表、统计、日期筛选和导出。

- 顶部统计只统计未过期活动。

- 日期筛选只显示今天及之后。
  - 日期按钮为两行 UI。
  - 第一行显示日期，例如 `6/6 周六`。
  - 第二行显示数量，例如 `7 条 / 1 通过`。

- 来源筛选已预留多来源：
  - 豆瓣
  - 活动行
  - 小红书
  - 公众号
  - 当前只有豆瓣有数据，其他来源按钮禁用。

- 城市筛选：
  - 当前有 `全部城市 / 北京 / 上海`。
  - 城市筛选会联动顶部统计、来源筛选、类型筛选、日期筛选、列表内容和顶部元信息。
  - 城市切换时保留同一份审核状态存储，不会另外拆一套页面。

- 排序：
  - 默认按来源原始顺序。
  - 保留活动时间排序。
  - 没有 `Zup 推荐排序`。
  - 已拒绝活动不会被自动排到最后；需要用状态筛选查看。

- 图片完整显示。
  - 使用 `object-fit: contain`。
  - 不裁切封面。

## 抓取方式与人工接管点

当前原则：优先通过内置浏览器读取用户可见页面 DOM，不要把第三方平台当成可无限制命令行抓取的数据接口。遇到登录、真人识别、访问异常时，让用户在内置浏览器里接管处理，处理完再继续读取页面。

豆瓣现在会对命令行访客请求触发异常访问拦截：

`有异常请求从你的 IP 发出，请登录使用豆瓣`

因此不要默认用 `curl` 或 Node `fetch` 以访客身份抓豆瓣。当前可用方式是通过 Codex 内置浏览器读取已登录页面。

活动行等其他来源也应优先走内置浏览器。如果活动行触发 IP 封禁、登录、真人识别或安全页，不要继续用命令行重试；停在可见浏览器页面，让用户处理网络/登录/识别后再读取 DOM。

豆瓣抓取流程：

1. 用户在 Codex 内置浏览器中登录豆瓣。
2. 打开 `https://shanghai.douban.com/events/week-all`。
3. Codex 使用 Browser/Node REPL 读取当前页面 DOM。
4. 从列表页读取活动卡片。
5. 逐条打开详情页读取 `#edesc_s`。
6. 清洗详情，只保存摘要，不保存原文。
7. 写入 `data/crawled-events.json` 和 `data/crawled-events.js`。

如果当前审核台已经是多城市模式，新增城市时不要覆盖整个 `events` 数组。正确做法是把新城市数据追加进现有数据包，并同步更新：

- `cities`
- `cityMeta`
- `sourcePages`
- `events`

遇到以下情况需要让用户接管内置浏览器：

- 豆瓣跳转到登录页。
- 出现真人识别、验证码、异常访问提示。
- 页面要求手动登录或确认。
- 页面内容为空但浏览器可见正常页面。

不要尝试绕过真人识别；让用户在内置浏览器里完成操作后再继续读取页面。

## 活动简介写作规则

`body` 是面向用户看的活动介绍，不是审核备注。

这是后续所有来源都要遵守的硬规则，不只适用于豆瓣。无论数据来自豆瓣、活动行、小红书、公众号还是人工录入，`body` 都应该像真实运营内容里的活动简介，而不是爬虫日志、审核备注、平台原文复制，或站在旁边点评活动的第三人称摘要。

必须遵守：

- 不写时间、地点、费用的重复说明，页面已有独立字段。
- 不写“默认拒绝”“适合/不适合 Zup”“人工判断”等审核话术。
- 口吻要像活动发布页里的正式介绍，要把人往活动里带，而不是站在外面评价活动。
- 不要写“这是一场……”“适合想……的人”“适合什么兴趣的人”“如果你喜欢……”这类旁白式、导购式、平台编辑式句子。
- 不要每条都套同一个句式。
- 不要保留票务规则、限购、退换、入场规则、儿童购票、订单说明等平台文本。
- 不保存原文长文，只做精炼摘要。
- 不超过 400 字，但不要为了短而短；信息丰富的活动可以写到 150-300 字。
- 摘要必须基于活动本身：主题、内容、形式、氛围、亮点、现场体验和参与感。
- 如果详情页里主要是票务/报名/退款/核销/入场说明，要过滤掉这些内容，只保留能帮助用户理解活动本身的信息。
- 如果原始详情很短或很像票务入口，可以用标题、分类、主办方和场地信息生成发布型简介，但不能编造不存在的演出阵容、嘉宾、权益或活动流程。
- 不要把抓取来源的风险、版权判断、平台限制、规则分数写进 `body`。

可以写：

- 活动主题和核心体验。
- 现场会发生什么，观众/参与者能看到、听到、参与到什么。
- 活动有什么看点，为什么此刻值得去。
- 现场形式，如展览、演出、分享、手作、社交、户外等。
- 如果原始页面信息很票务化，就基于标题、类型和已知内容写一个更有吸引力的活动介绍，不要夹带审核结论。

推荐口吻：

- 像活动发布者在认真介绍这场活动。
- 句子里优先写活动本身，而不是写“你适不适合去”。
- 尽量让读者对现场产生画面感、好奇心和参与冲动。

不推荐写法示例：

- “这是一场适合想放松的人参加的活动。”
- “适合喜欢艺术和社交的人群。”
- “如果你想找点周末活动，可以来看看。”

推荐写法示例：

- “剧场把情绪、节奏和近距离互动一起推到台前，笑点和停顿都更贴着现场发生。”
- “展厅从视觉、声音和空间氛围一路展开，走进去之后会更容易沉到作品想营造的世界里。”
- “这一晚的重点不是把流程走完，而是把人真正带进现场的节奏、气味、声音和交流里。”

审核判断应写在 `reviewReason`，不要写进 `body`。`reviewReason` 可以写“偏行业/商业”“像票务商品”“不适合 Zup 冷启动”等；`body` 不能写这些。

## 2026-06-03 多城市事故与修复

2026-06-03 发生过一次错误操作：用户原本是要“增加上海城市数据并在页面里加城市筛选”，但实际执行时把 `data/crawled-events.json` / `data/crawled-events.js` 直接覆盖成了上海单城市数据，导致原先北京数据在审核台里消失。

这次错误之后已经做了这些修复：

- 审核台加上了城市筛选，当前支持 `全部城市 / 北京 / 上海`。
- 数据文件已改成多城市合并结构，不再只支持单个 `city`。
- 北京数据先从本机临时快照里恢复回了 10 条。
- 上海数据保留为 30 条。
- 页面实测可切城市：北京 10 条，上海 30 条，全部城市 40 条。

后续必须遵守：

- 用户说“增加一个城市”，默认语义是“保留已有城市并追加新城市”，不是替换现有数据。
- 除非用户明确要求覆盖某个城市或清空旧数据，否则不能把 `events` 整包重写成单城市结果。
- 抓新城市前先检查当前 `data/crawled-events.json` 是否已经有其他城市数据。
- 如果需要补抓北京剩余数据，也要继续在当前多城市结构上追加或替换北京分组，不能再影响上海。

## 当前 UI 状态摘要

审核台当前已经具备：

- 单列活动列表。
- 完整封面图显示。
- 城市筛选。
- 来源字段和来源筛选。
- 状态筛选：全部、推荐、已通过、已拒绝、待定。
- 类型筛选。
- 日期筛选，显示每天总数和已通过数。
- 搜索。
- 来源原始顺序 / 活动时间排序。
- 本地持久化审核状态。
- 已通过列表导出。

## 注意事项

- 当前 JSON 方式适合小批量验证；多来源、多城市、大量去重时再考虑数据库。
- 第三方平台内容、图片、详情文本都有平台规则和版权风险。当前流程用于本地人工审核和产品验证，不应直接大规模搬运。
- 豆瓣当前只是测试样本来源；新增来源时不要默认复用豆瓣的页面结构、字段命名或抓取方式，要按来源单独适配。
- 如果新增来源，务必设置：
  - `source`
  - `sourceName`
  - `sourcePosition`
  - `originalLink`
  - `eventDates`
  - `body`
  - `reviewReason`
