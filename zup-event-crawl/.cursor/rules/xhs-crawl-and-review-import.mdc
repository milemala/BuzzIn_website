---
description: 抓取/处理小红书一周活动汇总并入库审核台。涉及 XHS 图片裁切、vision-slots、posterBox 时按强视觉模型一次标最终框 + 总览图验收流程执行。
globs: zup-event-crawl/**
alwaysApply: false
---

# 小红书活动：抓取 → 原尺寸读图标框 → 裁切验收 → 入库

当用户说「抓取/重跑/处理 XX 小红书活动」「小红书入库」「小红书一周汇总」，或编辑 `data/scrape-cache/xhs/**`、`vision-slots.json` 时：

**先读，不依赖聊天记忆**：

1. [`docs/xiaohongshu-review-workflow.md`](../docs/xiaohongshu-review-workflow.md) — **主流程与检查清单**
2. [`docs/xiaohongshu-vision-agent.md`](../docs/xiaohongshu-vision-agent.md) — Agent 读图与 posterBox
3. [`docs/xiaohongshu-weekly-crawl.md`](../docs/xiaohongshu-weekly-crawl.md) — 抓取与目录约定

账号清单：`data/xhs-city-accounts.json`

## 最佳实现路径

### 1. 抓取或定位笔记目录

```bash
cd zup-event-crawl
node scripts/run-xhs-weekly-pipeline.js --city=<城市名>
```

同笔记已抓过会跳过重复下载。若用户只是“重跑裁切”，先定位 `data/scrape-cache/xhs/<城市>/<笔记ID>/`，不要重复抓取。

### 2. 强视觉模型读图并一次标最终 `posterBox`

在 `data/scrape-cache/xhs/<城市>/<笔记ID>/`：

- 读 `images/*.webp`（从 `01.webp` 起）；如果不能直接读，生成**同尺寸临时 PNG**到 `_tmp-review-png/` 辅助读图，临时图不用强制删除，可保留复盘
- 写/改 `vision-slots.json`：每场活动都要有字段；只有版面里独立、尺寸足够、能干净裁成封面的主视觉才写 `posterBox`
- `posterBox` 是最终裁切区域，不是粗框：只框海报本体，排除 slide 背景、黄色标题条、右侧说明文字、邻场残影
- 优先使用强视觉模型（如 Fable）。弱视觉模型/模板化模型容易把“行/卡片/说明文字”当海报，不要信模板坐标
- **禁止**复制上周 box、固定比例裁切、按城市写启发式自动猜框
- 不要按图片内容意义过度苛刻：风景照、展品照、现场照、地图式活动图，只要在版面中是该活动的独立主视觉且尺寸够，就算可裁封面

### 3. 裁切 + 总览图验收（默认不入库）

```bash
node scripts/extract-xhs-weekly-events.js data/scrape-cache/xhs/<城市>/<笔记ID>
node scripts/create-poster-contact-sheet.js data/scrape-cache/xhs/<城市>/<笔记ID>
```

必须看 `posters-contact-sheet.png`：把所有 `posters/*.jpg` 放在一张缩略总览图里统一判断。只抓明显坏图，不逐张深挖复核。

如果总览图发现明显坏图，只允许**返工一次**：回原 slide 改 `posterBox` → 重跑 extract → 重跑 contact sheet。第二次总览后不再无限微调，除非用户明确要求继续。

必须回原 slide 重标的情况：

- 裁切里出现活动标题、黄色星标、右侧闹钟/地图/票价/说明文字
- 裁切包含大块 slide 背景或邻场内容
- 海报标题/日期/脚标明显被切掉
- 同一页多个 crop 明显是套模板而不是贴海报外缘

### 4. 像素吸附是可选修边，不是默认流程

只在“语义正确、边缘差几像素”时预览：

```bash
node scripts/snap-poster-box-edges.js data/scrape-cache/xhs/<城市>/<笔记ID>
```

确认预览合理才写回：

```bash
node scripts/snap-poster-box-edges.js data/scrape-cache/xhs/<城市>/<笔记ID> --write
```

不要用吸附修语义错误；它救不了框错块、框进标题/说明文字。

### 5. 满意后再入库

- `source=xiaohongshu`，`append-city`，不删同城豆瓣
- **入库阶段不做 POI**（POI 在分类/介绍之后由 Agent 单独做，见下节）
- 审核台：http://127.0.0.1:8787/ 来源选「小红书」

```bash
node scripts/run-xhs-weekly-pipeline.js --skip-scrape --city=<城市名>
```

### 6. 校正入库时间（入库后必做）

见 [`docs/event-time-agent.md`](../docs/event-time-agent.md)：

```bash
node scripts/suggest-event-time-decisions.js --source=xiaohongshu --all-cities
# Agent 核对 time-decisions.json
node scripts/apply-event-time-decisions.js --city=多城市
```

### 7. 后续（与豆瓣相同）

- 分类：[`docs/event-classification-agent.md`](../docs/event-classification-agent.md)
- 介绍：[`docs/event-body-agent.md`](../docs/event-body-agent.md)
- **POI（Agent 全程主导）**：[`docs/event-poi-agent-workflow.md`](../docs/event-poi-agent-workflow.md)

```bash
node scripts/export-events-for-poi.js --city=<城市> --source=xiaohongshu --refresh --pending-only
# 或 --doubtful-only 复核存疑 → doubtful-pending.json
# Agent：每组定搜词 → poi-search-cli.js → 读候选 → 手写 decisions.json
node scripts/apply-event-poi-decisions.js --file=data/poi-agent-workbench/<城市>-xhs/decisions.json
```

搜词与 match/reject/doubtful **必须由 Agent 读候选后手写 decisions**，禁止 JS 自动批处理（含已删除的 `build-agent-rerun-decisions.js`、`agent-poi-analyze-doubtful.js`）。

POI 判定硬规则见 workflow「判断标准」：**主体一致仅差精度 → match 且不存疑**；**多校区/无法唯一确定 → reject**。

## 禁止

- 用启发式/城市规则自动猜海报位置
- 把像素吸附作为默认流程或不预览直接写回
- 入库用 `merge-city` / `replace-city`（会删同城豆瓣活动）
- 抓取/入库阶段做 POI、`--with-poi`、或 JS 自动选点（`pickBestPoiForEvent` 跑批写库）
- 无 `posterBox` 时硬裁文字 slide 或装饰图
- 依赖对话记忆而不读 `docs/xiaohongshu-review-workflow.md`
- 用低分辨率预览量最终坐标；临时图必须同尺寸
- 反复抽查、反复微调导致无限返工；默认最多返工一次

## 交付口径

汇报：活动数、海报通过/丢弃/无海报数量、总览图路径、是否返工过、是否入库、是否还有需人工判断的坏图。不要只说“跑完了”。

## 封面规则

| 有 poster | 无 poster |
|-----------|-----------|
| 豆瓣同款 4:3 左图右文 | `lib/xhs-text-cover-compose.js` 文字封面 |
| `image_original` = 裁切海报 | `image_original` = 原 slide |
