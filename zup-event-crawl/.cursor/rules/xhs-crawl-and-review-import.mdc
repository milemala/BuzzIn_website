---
description: 抓取/处理小红书一周活动汇总并入库审核台；入库后同会话须完成 Agent 分类与 POI。涉及 vision-slots、posterBox 时按读图标框流程执行。
globs: zup-event-crawl/**
alwaysApply: false
---

# 小红书活动：抓取 → 动态读图标框 → extract → 入库

## 一站式分工（必读）

| 谁 | 做什么 | 不做什么 |
|----|--------|----------|
| **用户** | 用自然语言提需，如「抓取重庆小红书」「重跑重庆海报」「入库并做分类 POI」 | 不运行脚本、不读技术文档、不拼 prompt、不记命令 |
| **执行 Agent** | 读本文 → 动态读图标框/extract/入库/分类/POI → **汇报** | 不跑红框预览、不拼 contact sheet；禁止子代理批量套坐标 |

**向用户汇报时**：说清做了什么、活动数/海报情况/是否入库/有无待决问题即可。技术路径（跑了哪些脚本、文件路径）仅在被问到或排查故障时再展开。

用户提需口语示例：[`AGENTS.md`](../AGENTS.md)「用户怎么说」。

---

当用户说「抓取/重跑/处理 XX 小红书活动」「小红书入库」「小红书一周汇总」，或编辑 `data/scrape-cache/xhs/**`、`vision-slots.json` 时：

**硬性门禁（违反即停止，不得交付）**：仓库根 [`.cursor/rules/zup-event-crawl-hard-gates.mdc`](../../.cursor/rules/zup-event-crawl-hard-gates.mdc) — 海报 `posterBox` 须读图标框；禁止像素脚本；`extract` 有 posterBox 守卫。

**先读，不依赖聊天记忆**：

0. [`docs/event-crawl-master-workflow.md`](../docs/event-crawl-master-workflow.md) — **豆瓣/小红书总流程与 JS/Agent 分工**
1. [`docs/xiaohongshu-review-workflow.md`](../docs/xiaohongshu-review-workflow.md) — **主流程与检查清单**
2. [`docs/xiaohongshu-vision-labeling-prompt.md`](../docs/xiaohongshu-vision-labeling-prompt.md) — **Agent 读图标框提示词（定稿，标框前必读）**
3. [`docs/xiaohongshu-vision-agent.md`](../docs/xiaohongshu-vision-agent.md) — Agent 读图与 posterBox
4. [`docs/xiaohongshu-poster-crop-rules.md`](../docs/xiaohongshu-poster-crop-rules.md) — **海报裁切精度定稿（标框前必读）**
4. [`docs/xiaohongshu-weekly-crawl.md`](../docs/xiaohongshu-weekly-crawl.md) — 抓取与目录约定
4. [`docs/event-classification-agent.md`](../docs/event-classification-agent.md) — 入库后分类
5. [`docs/event-poi-agent-workflow.md`](../docs/event-poi-agent-workflow.md) — **入库后 POI（Agent 定搜词、读候选、写 decisions）**

账号清单：`data/xhs-city-accounts.json`

## 最佳实现路径

### 1. 抓取或定位笔记目录

```bash
cd zup-event-crawl
node scripts/run-xhs-weekly-pipeline.js --city=<城市名>
```

同笔记已抓过会跳过重复下载。若该 noteId **已入库 review.db**，整条流水线（读图、裁切、入库、分类、POI）**全部跳过**，即使账号重新编辑了原帖也不重做。

**选帖后必看详情日期**：标题初选命中后，脚本会打开详情页校验；正文/`活动周期` 里若全是已过期区间（如 `6.19-6.21`），自动跳过并试下一帖。Agent 手动介入时，若日期只在**封面图**上、正文没有，读第一张 slide——一眼过期则**放弃该城**，不要读图标框入库。

### 2. 读图并标 `posterBox`（动态）

1. 读 labeling-prompt + poster-crop-rules
2. 根据版式复杂度决定一次读几张 `images-jpg/XX.jpg`，标完当前这组所有场次
3. 同页三场：`y` 按行分别量；禁止整帖一气读完再写坐标、禁止子代理并行合并
4. 写 `vision-slots.meta.json` → `extract` → `import`

### 3. extract + 入库（无红框、无大图）

```bash
node scripts/extract-xhs-weekly-events.js data/scrape-cache/xhs/<城市>/<笔记ID>
```

**不跑** `preview-poster-boxes.js`、`create-poster-contact-sheet.js`。

### 4. 像素吸附是可选修边，不是默认流程

只在“语义正确、边缘差几像素”时预览：

```bash
node scripts/snap-poster-box-edges.js data/scrape-cache/xhs/<城市>/<笔记ID>
```

确认预览合理才写回：

```bash
node scripts/snap-poster-box-edges.js data/scrape-cache/xhs/<城市>/<笔记ID> --write
```

不要用吸附修语义错误；它救不了框错块、框进标题/说明文字。

### 5. Agent 验收通过后入库

- `source=xiaohongshu`，`append-city`，不删同城豆瓣
- **入库阶段不写 POI**；入库后**同会话** Agent 做分类 + POI（脚本会自动导出 pending 文件）
- 审核台：http://127.0.0.1:8787/ 来源选「小红书」

```bash
node scripts/run-xhs-weekly-pipeline.js --skip-scrape --city=<城市名>
```

### 6. 校正入库时间（入库后必做）

见 [`docs/event-time-agent.md`](../docs/event-time-agent.md)：

```bash
node scripts/suggest-event-time-decisions.js --source=xiaohongshu --all-cities
# Agent 核对 time-decisions.json
node scripts/apply-event-time-decisions.js --city=多城市
```

### 7. 分类（入库后同会话必做）

入库脚本会自动导出 `data/poi-agent-workbench/<城市>-xhs/classification-pending.json`。

**同一次对话内**，Agent 读完 pending 后手写 `classification-decisions.json` 并 apply（禁止只靠 JS 正则批处理代替读文判断）：

```bash
node scripts/export-events-for-classification.js --city=<城市> --source=xiaohongshu   # 入库已自动导出时可跳过
# Agent 写 classification-decisions.json
node scripts/apply-event-classification-decisions.js --city=<城市> --source=xiaohongshu
```

规则：[`docs/event-classification-agent.md`](../docs/event-classification-agent.md)

### 8. POI 匹配（分类后同会话必做）

入库脚本会自动导出 `data/poi-agent-workbench/<城市>-xhs/pending.json`（按地址去重组，含 `cached_poi` 若有）。

**同一次对话内**，对 `pending.json` 的**每一个** `group`：

1. 有 `cached_poi` → 直接写 `match` decisions，不必搜腾讯
2. 否则 Agent **自己定** 1～3 个搜词（场馆/门牌，不用活动标题）
3. `node scripts/poi-search-cli.js --city=<城市> --keyword="..."`（或 `--location=` 先查映射库）
4. Agent **读** `items[]`，按 workflow「判断标准」手写 `match` / `reject` / `doubtful`
5. 写入 `decisions.json` → apply

```bash
node scripts/export-events-for-poi.js --city=<城市> --source=xiaohongshu --refresh --pending-only --new-import-only   # 入库已自动导出时可跳过；补历史 backlog 去掉 --new-import-only
# Agent 写 data/poi-agent-workbench/<城市>-xhs/decisions.json
node scripts/apply-event-poi-decisions.js --city=<城市> --source=xiaohongshu
```

规则：[`docs/event-poi-agent-workflow.md`](../docs/event-poi-agent-workflow.md)

**禁止** JS 自动选点、`pickBestPoiForEvent` 跑批写库、`batch-resolve-event-poi.js`（已删除）。

### 9. 汇报

**入库不是终点。** 向用户汇报「抓取/处理完成」前，必须已完成本节第 7、8 步（分类 + POI）；缺一不可。

抓取入库后同会话须汇报：**分类**（推荐/挡下条数）+ **POI**（处理几组、匹配/reject/存疑几组）。

**禁止**仅汇报「已入库/已抓完」而未做分类与 POI 就结束会话。

## 禁止

- **用 Node/像素/启发式脚本判断并批量写入 `posterBox`**（`poster-box-percent-to-px.js` 仅允许 %→px 算术）
- **跑红框预览**（`preview-poster-boxes.js`）
- **拼海报大图**（`create-poster-contact-sheet.js`）
- **子代理并行批量写坐标**（套模板）
- **无 `vision-slots.meta.json` 时跑 extract**（守卫会 exit 3）
- 用启发式/城市规则自动猜海报位置
- 把像素吸附作为默认流程或不预览直接写回
- 入库用 `merge-city` / `replace-city`（会删同城豆瓣活动）
- 抓取/入库阶段做 POI、`--with-poi`、或 JS 自动选点（`pickBestPoiForEvent` 跑批写库）
- 无 `posterBox` 时硬裁文字 slide 或装饰图
- 依赖对话记忆而不读 `docs/xiaohongshu-review-workflow.md`
- 用低分辨率预览量最终坐标；读图 JPEG 必须与原 webp **同尺寸**
- 占比太小的缩略图硬标 posterBox

## 交付口径

汇报：活动数、海报数/无海报数、是否入库、**分类**、**POI**。

## 封面规则（定稿，勿随意改）

| 情况 | 版式 | 实现 |
|------|------|------|
| 有 poster，竖图 ≤4:5 | 4:3：模糊底图 + 左海报 + 右**标题 + 加入群聊/一起组局**（江城律动圆、白字） | `lib/event-image-compose.js` |
| 有 poster，宽图 >4:5 | 4:3：模糊底图 + 海报居中，无右侧文案 | 同上 |
| 无 poster | 随机 `background1~6.jpg` + **标题 + 加入群聊/一起组局**（江城律动圆、深色字） | `lib/xhs-text-cover-compose.js` |

`image_original`：有 poster 时 = 裁切海报；无 poster 时 = 原 slide。
