---
description: 用户要抓取某城豆瓣活动时：抓取入库 + Cursor 大模型完成 POI 搜索与判断（禁止走 JS 批量 POI 脚本）
globs: zup-event-crawl/**
alwaysApply: false
---

# 豆瓣活动：抓取 + Agent POI（标准流程）

当用户说「抓取/抓一下 XX 城市豆瓣活动」或类似需求时，**在同一次对话里完成抓取与 POI**，不要只跑抓取就结束。

## 禁止

- 不要跑 `run-city-agent-poi.js`（已废弃的 JS 自动 POI 流水线）
- 不要跑 `agent-poi-build-search-plan.js` / `agent-poi-batch-search.js` / `agent-poi-build-decisions.js`
- 抓取时不要加 `--with-poi`（旧版 JS Top1）
- 不要用 `tencent-poi.js` 的 `pickBestPoiForEvent` / `suggestEventPoiKeyword` 代替大模型做最终判断

## 必须执行的步骤

### 1. 抓取入库（机械）

```bash
cd zup-event-crawl
node scripts/scrape-douban-week-events.js 500 data/review.db --city=<slug> --mode=append-city --max-pages=10
```

### 2. 导出待匹配 POI 任务

```bash
node scripts/export-events-for-poi.js --city=<城市中文名> --refresh --pending-only
```

生成 `data/poi-agent-workbench/<城市>/pending.json`（按地址去重后的 `groups`）。

### 3. POI：由本对话中的大模型完成（核心）

对每个 `group`：

1. 读 `location`、`sample_title`，决定 1～3 个腾讯搜索词（见 `docs/event-poi-agent-workflow.md`）
2. 执行 `node scripts/poi-search-cli.js --city=<城市> --keyword="..."`
3. 对比豆瓣原文与候选 JSON，选定 `poi_id` 或 `reject`（未匹配 POI，审核仍保持待审核），写入 `doubtful` / `reason`

搜词原则摘要：

- 用场馆/商场/门牌，不用活动标题
- 保留栋/座/楼
- `沉浸式…《剧名》-锈罐头剧场` → 搜横杠后的店名
- 区划不符、仅命中停车场/地铁站 → reject

### 4. 写 decisions 并入库

写入 `data/poi-agent-workbench/<城市>/decisions.json`，然后：

```bash
node scripts/apply-event-poi-decisions.js --city=<城市>
```

### 5. 汇报

告知：抓取条数、POI 匹配/未匹配/存疑数量、需人工看的条目。

## 审核页角色

活动审核台只**展示** Agent 写入的 POI 与存疑标记；人工可「自由搜索 + 点选」修正，但不再依赖 JS 自动搜词/自动 Top1。
