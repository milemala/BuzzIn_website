---
description: 抓取豆瓣活动 + Cursor 大模型全流程（时间/分类/body/POI）。用户说「抓取某城豆瓣活动」时必读 docs/event-crawl-master-workflow.md 与本文。
globs: zup-event-crawl/**
alwaysApply: false
---

# 豆瓣活动：抓取 + Agent POI（标准流程）

当用户说「抓取/抓一下 XX 城市豆瓣活动」、补 POI、重搜存疑活动，或编辑 `poi-agent-workbench/**` 时：

**先读**：

0. [`docs/event-crawl-master-workflow.md`](../docs/event-crawl-master-workflow.md) — **总流程与 JS/Agent 分工**
1. [`docs/event-time-agent.md`](../docs/event-time-agent.md) — 入库时间（抓取后第一步）
2. [`docs/event-classification-agent.md`](../docs/event-classification-agent.md) — 推荐/挡下 + 分类
3. [`docs/event-body-agent.md`](../docs/event-body-agent.md) — 活动介绍 body
4. [`docs/event-poi-agent-workflow.md`](../docs/event-poi-agent-workflow.md) — POI 匹配

**不要依赖聊天记忆。**

## POI 铁律（Agent 全程主导）

**搜词、读候选、match/reject/doubtful 判定必须由 Cursor Agent 在对话里完成**，不得交给 JS 自动规则：

1. Agent 读 `pending.json` 每组 → **自己定** 1～3 个搜词
2. `node scripts/poi-search-cli.js --city=... --keyword=...` → Agent **自己读** JSON 候选
3. Agent 按 workflow「判断标准」写 `decisions.json`
4. `node scripts/apply-event-poi-decisions.js` 入库

`buildEventPoiSearchKeywords` / `pickBestPoiForEvent` 等仅供 Agent **参考**，不能代替 Agent 写 `decisions.json`。

**处理范围**：仅 `export-events-for-poi.js --pending-only`（审核 **待定** + 无 POI）。用户已在审核台 **拒绝** 的活动（`review_decisions.status=rejected`）**不要处理**。

**POI `reject` ≠ 审核台「拒绝」**：前者只是本轮没配上 POI，活动仍待定；后者是不要这条活动。

## 禁止

- `batch-resolve-event-poi.js`、`agent-poi-review-apply.js`、`reassess-agent-poi-doubt.js`（已删除）
- `agent-poi-analyze-doubtful.js`、`build-agent-rerun-decisions.js`（已删除；JS 自动改存疑/生成 decisions）
- `run-city-agent-poi.js` 及任何 `agent-complete-*` / `agent-poi-build-*` 类 JS 自动流水线
- 抓取加 `--with-poi`；`batchEventAutoPoi`；审核页批量 JS 自动 POI
- 用 `pickBestPoiForEvent` / `buildEventPoiSearchKeywords` **自动跑批**并写 `decisions.json` 或 `review.db`
- 用 JS 比地址文字批量写 `doubtful` 或批量生成 `decisions.json`（必须 Agent 读 `poi-search-cli` 候选后手写）

## 必须执行的步骤

### 1. 抓取入库

```bash
cd zup-event-crawl
node scripts/scrape-douban-week-events.js 500 data/review.db --city=<slug> --mode=append-city --max-pages=10
```

或一键：`node scripts/prepare-city-poi-for-agent.js --city=<中文城市名> --max-pages=10`

抓取脚本**只写 time_text**，不写最终 `start_date`/`end_date`。

### 2. 校正入库时间（在分类之前）

```bash
node scripts/export-events-for-time.js --city=<中文城市名> --source=douban
```

读 `time-pending.json`，核对/修改 `time-decisions.json`（可先跑 `suggest-event-time-decisions.js` 生成草稿）。仅开始时刻 → `expired_at` 当天 23:59:59。

```bash
node scripts/apply-event-time-decisions.js --city=<中文城市名>
```

### 3. 导出并做分类/挡下（在 POI 之前）

```bash
node scripts/export-events-for-classification.js --city=<中文城市名> --refresh
```

读 `classification-pending.json`，逐条写 `classification-decisions.json`（`suggested` + `category` + `reason`），挡下行业展会/贸易展等无趣活动。

```bash
node scripts/apply-event-classification-decisions.js --city=<中文城市名>
```

### 4. 导出并撰写活动介绍

```bash
node scripts/export-events-for-body.js --city=<中文城市名> --refresh
```

读 `body-pending.json`，逐条写 `body-decisions.json`（完整 `body` = 活动介绍 + 参加方式 + `reason`）。参加方式按原文判断：联系豆瓣发布者 / 公众号微信 / 票务 App。

```bash
node scripts/apply-event-body-decisions.js --city=<中文城市名>
```

无 Agent 时备选：`node scripts/batch-infer-event-bodies.js --city=<中文城市名>`

### 5. 导出 POI 任务

```bash
# 一条龙 / 只补未匹配
node scripts/export-events-for-poi.js --city=<中文城市名> --refresh --pending-only

# 用户只要求复核存疑
node scripts/export-events-for-poi.js --city=<中文城市名> --refresh --doubtful-only
```

读 `pending.json`（未匹配）或 `doubtful-pending.json`（存疑复核）。

### 6. Agent POI（核心，禁止 JS 代劳）

对导出文件里**每一个** `groups[]` 条目：

1. **Agent 定** 1～多个搜词（场馆/门牌/栋座；不够准就多试）
2. **Agent 执行** `node scripts/poi-search-cli.js --city=<城市> --keyword="..."`
3. **Agent 读** `items[]`，按 workflow「判断标准」**手写** `match` / `reject` / `doubtful` + `reason`
4. 存疑复核：必须重新搜，不能只看 `current_poi_*` 就改标记

不得用 JS 自动生成 `decisions.json`；不得跳过第 2～3 步。

**硬规则**（详见 workflow「判断标准」）：

- 区划一致；泛名「文创园/产业园」无门牌 → 不 match
- **主体一致、仅差楼层/铺位/室号** → `match` + `doubtful: false`（如商场主体、同门牌楼宇）
- **多校区/多分店/地址过泛无法唯一确定** → `reject`，禁止猜默认校区或第一家店
- 除「仅差精度」外仍有跨区、铺位不同、类目不符等 → `match` + `doubtful: true`

### 7. 写 POI decisions 并入库

`data/poi-agent-workbench/<城市>/decisions.json`（**Agent 手写**）→

```bash
node scripts/apply-event-poi-decisions.js --city=<城市>
# 多城批次：merge-agent-poi-decisions.js --file=...
```

### 8. 汇报

抓取条数、时间/分类/body 条数；POI：**处理了几组**、匹配/reject/仍存疑；需人工条目。

## 审核台

- 展示 Agent POI；POI 存疑只认 `poi_agent_doubtful` / `poi_agent_reason`
- 人工修正：自由搜索 + 点选（`manual`）
