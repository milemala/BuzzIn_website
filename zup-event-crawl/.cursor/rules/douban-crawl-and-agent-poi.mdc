---
description: 抓取豆瓣活动 + Cursor 大模型 POI 匹配。用户说「抓取某城活动」时必读 docs/event-poi-agent-workflow.md 并按其中检查清单执行。
globs: zup-event-crawl/**
alwaysApply: false
---

# 豆瓣活动：抓取 + Agent POI（标准流程）

当用户说「抓取/抓一下 XX 城市豆瓣活动」、补 POI、重搜存疑活动，或编辑 `poi-agent-workbench/**` 时：

**先读**：

1. [`docs/event-classification-agent.md`](../docs/event-classification-agent.md) — 推荐/挡下 + 分类
2. [`docs/event-body-agent.md`](../docs/event-body-agent.md) — 活动介绍 body
3. [`docs/event-poi-agent-workflow.md`](../docs/event-poi-agent-workflow.md) — POI 匹配

**不要依赖聊天记忆。**

## 禁止

- `run-city-agent-poi.js`（已废弃占位）及任何 `agent-complete-*` / `agent-poi-build-*` 类 JS 自动流水线（已从仓库删除）
- 抓取加 `--with-poi`
- 用 `pickBestPoiForEvent` / `suggestEventPoiKeyword` 代替大模型写 `decisions.json`
- 用 `reassess-agent-poi-doubt.js` 把 `action:reject` 的组强行绑 POI

## 必须执行的步骤

### 1. 抓取入库

```bash
cd zup-event-crawl
node scripts/scrape-douban-week-events.js 500 data/review.db --city=<slug> --mode=append-city --max-pages=10
```

或一键：`node scripts/prepare-city-poi-for-agent.js --city=<中文城市名> --max-pages=10`

### 2. 导出并做分类/挡下（在 POI 之前）

```bash
node scripts/export-events-for-classification.js --city=<中文城市名> --refresh
```

读 `classification-pending.json`，逐条写 `classification-decisions.json`（`suggested` + `category` + `reason`），挡下行业展会/贸易展等无趣活动。

```bash
node scripts/apply-event-classification-decisions.js --city=<中文城市名>
```

### 3. 导出并撰写活动介绍

```bash
node scripts/export-events-for-body.js --city=<中文城市名> --refresh
```

读 `body-pending.json`，逐条写 `body-decisions.json`（完整 `body` = 活动介绍 + 参加方式 + `reason`）。参加方式按原文判断：联系豆瓣发布者 / 公众号微信 / 票务 App。

```bash
node scripts/apply-event-body-decisions.js --city=<中文城市名>
```

无 Agent 时备选：`node scripts/batch-infer-event-bodies.js --city=<中文城市名>`

### 4. 导出 POI 任务

```bash
node scripts/export-events-for-poi.js --city=<中文城市名> --refresh --pending-only
```

读 `data/poi-agent-workbench/<城市>/pending.json`。

### 5. 大模型 POI（核心）

对每个 `group`：

1. 定 1～3 个搜词（场馆/门牌/栋座，非活动标题；笔误要纠）
2. `node scripts/poi-search-cli.js --city=<城市> --keyword="..."`
3. 按 workflow 文档「判断标准」选点：`match` / `reject` / `doubtful` + `reason`
4. 可选：把候选列表写入 `search-results.json` 便于审计

**硬规则**：区划必须一致；泛名「文创园/产业园」无门牌对齐 → 不 match；多场地（等N家影院）要标存疑或只绑主场地。

### 6. 写 POI decisions 并入库

`data/poi-agent-workbench/<城市>/decisions.json` →

```bash
node scripts/apply-event-poi-decisions.js --city=<城市>
```

### 7. 汇报

抓取条数、推荐/挡下/待分类、body 待写/已写、POI match/reject/doubtful、需人工条目。

## 审核台

- 展示 Agent POI；POI 存疑只认 `poi_agent_doubtful` / `poi_agent_reason`
- 人工修正：自由搜索 + 点选（`manual`）
